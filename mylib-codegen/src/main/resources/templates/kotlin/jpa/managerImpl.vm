package ${package}.manager.impl

import ${package}.dao.${entity.name}Dao
import ${package}.entity.${entity.name}
import ${package}.manager.${entity.name}Manager
import com.zy.mylib.data.jpa.BaseJpaManager

import javax.inject.Inject
import javax.inject.Named

/**
 * ${entity.caption} 管理类
 * @author 代码生成器
 */
@Named
open class ${entity.name}ManagerImpl: BaseJpaManager<${entity.name}Dao, ${entity.name}, String>(), ${entity.name}Manager {
}