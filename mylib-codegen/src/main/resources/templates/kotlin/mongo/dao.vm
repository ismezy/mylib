package ${package}.dao

import ${package}.entity.${entity.name}
import com.zy.mylib.mongo.repos.BaseMongoRepository

/**
 * ${entity.caption} mongo dao
 * @author 代码生成器
 */
interface ${entity.name}Dao : BaseMongoRepository<$entity.name, String> {
}